spinner(show='showSpinner') //- boolean scope property to toggle spinner
h3 Events
h5 drag mouse over an area to select
.row(style='padding-bottom:20px')
  .col-md-12
    .nav-chart-container(style='width:95%; height:160px')
      event-graph
.row
  .col-md-3
    table.table.table-striped
      thead
        tr
          th Events
      tbody(ng-repeat="selectedEvent in selectedEvents")
        tr
          td
            p(ng-click="eventSelected(selectedEvent)") {{selectedEvent.start_time_ms | date:'MMMM dd, yyyy h:mm'  }} - {{selectedEvent.end_time_ms | date:'MMMM dd, yyyy h:mm'  }}
  .col-md-4
    event-map(ng-model='eventPoints')
      iframe(src="http://localhost:3005/index.html", style="width:100%; height:600px;", scrolling="no", frameborder="1", id="map-frame")
  .col-md-5
    .row(ng-show='imageUrls' style='width:100%; height:100px')
      image-grid(hover-dir='top-left')
    .row
      p(ng-show='clusterTerm' style='font-size:1.5em') # {{clusterTerm}}
    .row(style='width:100%; height:600px')
      .cloud-container(style='width:100%; height:400px')
        word-cloud(ng-model='clusterText')
